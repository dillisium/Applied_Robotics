"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const bms_receiver_1 = require("./receivers/bms-receiver");
const robot_receiver_1 = require("./receivers/robot-receiver");
const messageReceivers = Object.assign(Object.assign({}, bms_receiver_1.default), robot_receiver_1.default);
function messageHandler(topic, message, data) {
    const msgTopic = topic;
    const dataView = new DataView(message.buffer, message.byteOffset, message.byteLength);
    const receiver = messageReceivers[msgTopic];
    if (receiver)
        receiver(data, message, dataView);
    //else console.log(message);
}
exports.default = messageHandler;
