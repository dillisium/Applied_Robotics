/// <reference types="node" />
import { Go1State } from "../go1-state";
import { FirmwareSubTopic } from "../topics";
type Receivers = {
    [key in FirmwareSubTopic]: (data: Go1State, message: Buffer, dataView: DataView) => void;
};
declare const receivers: Receivers;
export default receivers;
